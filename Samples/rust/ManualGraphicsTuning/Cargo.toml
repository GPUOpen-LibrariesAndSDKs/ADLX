[package]
name = "manual-graphics-tuning"
description = "ADLX Rust sample: read GPU manual graphics tuning (min/max frequency, voltage)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "ManualGraphicsTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
