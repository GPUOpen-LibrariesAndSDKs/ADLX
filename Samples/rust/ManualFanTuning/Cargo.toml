[package]
name = "manual-fan-tuning"
description = "ADLX Rust sample: read GPU manual fan tuning ranges and current settings"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "ManualFanTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
