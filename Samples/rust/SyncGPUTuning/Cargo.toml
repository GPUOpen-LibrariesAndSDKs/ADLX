[package]
name = "sync-gpu-tuning"
description = "ADLX Rust sample: receive synchronous GPU tuning change notifications"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "SyncGPUTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
