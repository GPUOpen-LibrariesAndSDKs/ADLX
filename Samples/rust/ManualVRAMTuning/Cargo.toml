[package]
name = "manual-vram-tuning"
description = "ADLX Rust sample: read GPU manual VRAM tuning (memory timing, max VRAM frequency)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "ManualVRAMTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
