[package]
name = "gpu-auto-tuning"
description = "ADLX Rust sample: query GPU automatic tuning (undervolt/overclock support and state)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "GPUAutoTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
