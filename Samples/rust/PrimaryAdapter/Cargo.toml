[package]
name = "primary-adapter"
description = "ADLX Rust sample: identify the primary adapter (GPU driving the primary desktop)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "PrimaryAdapter"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
