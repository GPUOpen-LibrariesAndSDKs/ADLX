[package]
name = "display-custom-color"
description = "ADLX Rust sample: query display custom color (hue, saturation, brightness, contrast, temperature)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "DisplayCustomColor"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
