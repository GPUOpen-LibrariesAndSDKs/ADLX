[package]
name = "smoke-tests"
description = "Smoke tests that build and run every ADLX Rust sample"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lib]
path = "src/lib.rs"
