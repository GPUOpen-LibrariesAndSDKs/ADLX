[package]
name = "display-gpu-scaling"
description = "ADLX Rust sample: query and toggle GPU scaling on each display"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "DisplayGPUScaling"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
