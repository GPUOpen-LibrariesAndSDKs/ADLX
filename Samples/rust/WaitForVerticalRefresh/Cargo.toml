[package]
name = "wait-for-vertical-refresh"
description = "ADLX Rust sample: query the Wait for Vertical Refresh (VSync) mode"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "WaitForVerticalRefresh"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
