[package]
name = "gpu-preset-tuning"
description = "ADLX Rust sample: query GPU preset tuning (power saver, quiet, balanced, turbo, rage)"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "GPUPresetTuning"
path = "src/main.rs"

[dependencies]
amd-adlx = { path = "../amd-adlx" }
